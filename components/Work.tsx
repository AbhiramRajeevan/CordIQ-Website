import { Reveal } from "./Reveal";
import Link from "next/link";
import { projects } from "@/lib/work-data";
import { ArrowIcon } from "./visuals/Icons";

export function Work() {
  return <section id="work" className="py-32"><div className="max-w-[1240px] mx-auto px-6 md:px-8"><Reveal><span className="font-mono text-[10px] tracking-[.16em] uppercase text-orange">Selected work</span><div className="mt-4 flex flex-col md:flex-row md:items-end md:justify-between gap-6"><h2 className="max-w-[700px] font-display font-bold tracking-[-.035em] text-[clamp(36px,5vw,62px)] leading-[1.02]">Useful technology, shipped with intent.</h2><p className="max-w-[360px] text-mid leading-relaxed text-sm">A few examples across software, infrastructure and product delivery. Details can stay confidential when they need to.</p></div></Reveal><div className="mt-16 space-y-4">{projects.map(p=><Reveal key={p.n} y={24}><article className="group grid lg:grid-cols-[1fr_1fr] gap-7 items-center rounded-3xl border border-line bg-bg1/70 p-6 md:p-8 hover:border-violetSoft transition-colors"><div><span className="font-mono text-[10px] tracking-[.14em] text-orange">{p.type}</span><h3 className="mt-3 font-display font-semibold text-[clamp(22px,2.5vw,32px)] leading-tight max-w-[600px]">{p.title}</h3></div><div><p className="text-mid text-sm leading-[1.7]">{p.body}</p><div className="mt-5 flex flex-wrap gap-2">{p.tags.map(t=><span key={t} className="rounded-full border border-line px-3.5 py-1.5 text-[12.5px] font-mono font-medium text-mid">{t}</span>)}</div></div></article></Reveal>)}</div><Reveal className="mt-10"><Link href="/work" className="group inline-flex items-center gap-2 rounded-full border border-line px-6 py-3.5 text-sm font-semibold hover:border-violetSoft hover:-translate-y-0.5 transition-all">View all case studies<ArrowIcon className="w-3.5 h-3.5 transition-transform group-hover:translate-x-1"/></Link></Reveal></div></section>;
}
